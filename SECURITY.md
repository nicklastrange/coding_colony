# Security

Do not commit secrets, API keys, private repository paths, private generated
task documents, or local harness auth files.

Secrets belong in `.env` or the native harness secret store. `.env` is ignored.

Report security issues privately to the project maintainer instead of opening a
public issue with sensitive details.

